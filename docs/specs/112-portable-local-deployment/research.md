# 研究记录：跨机器本地部署

**日期**：2026-09-21

**关联**：[Spec](spec.md)、[Issue #112](https://github.com/JLU-ICCMS-MaYuan/SC-Wiki/issues/112)

## 规划时核验的原有实现

| 证据 | 事实及影响 |
| --- | --- |
| `Makefile`、`scripts/setup-local.sh` | `setup` 和 `start` 分离；安装仍要求旧 `.env` 和既有环境，用户报错文本来自安装脚本，不能据此断言 `start` 本身执行了安装。 |
| `scripts/lib-local.sh` | 默认 `~/miniconda3/envs/sc-wiki`、`~/.local/go`；端口和配置存在本机假设。 |
| `scripts/setup-local.sh` | MySQL 8.4、Java 21；Neo4j 来自 Docker 镜像；Qdrant/Go 下载写死 Linux x86_64；未负责完整 Node/npm 安装。 |
| `scripts/dev.sh` | 编排 11 个服务，GROBID 实际依赖 Docker；Python 启动前运行迁移；不能沿用旧文档的“完全移除 Docker”表述。 |
| `scripts/migrate-from-docker.sh` | 包含固定 `/home/mayuan/work/SC-Wiki-docker` 路径和旧数据卷名称，只适用于旧迁移。 |
| `backend/ingest/upload_tasks.py` | Redis 保存上传状态、草稿和短期用户模型凭据；不能整库丢弃，也不能原样搬运整个 Redis。 |
| `backend/rag/llm_context.py` | `.data/runtime/default_llm.json` 含 API Key；排除 `.env` 并不足以排除所有凭据。 |
| `backend/api/upload_tasks.py` | 上传清单同时保存绝对 `file_path` 和相对 `source_file_path`；换根路径需要字段级转换。 |
| `backend/scripts/run_migrations.py` | 默认升级到历史 `issue90_copy_v1`，特定变量下使用单数 `head`；既有库无版本时会 stamp 基线。这些行为不可直接作为恢复包启动契约。 |
| `alembic/versions/` | 静态迁移图有 `20260914_0052`、`20260918_0108` 两个头；不能强制套用单头测试。此为源码事实，未读取当前业务库版本。 |
| `backend/scripts/init_db.py` | 命令主体填充元素，只有专用 SQLite 函数执行 `create_all`，不能误称它会初始化 MySQL 全部结构。 |
| `tests/02_maintenance_and_verification/test_fresh_mysql_schema.py` | 仍包含单头假设；本 Feature 必须调整相应验收为已审定头集合，不删除多分支保护。 |
| `docker/requirements.txt`、`requirements.txt` | 两份依赖存在差异；本地安装当前使用前者。采用现有运行依赖入口，不顺便进行全仓依赖重构。 |

上表记录规划时的原有实现。后续实施使用人工数据和隔离实例验证，不访问当前业务表内容、
不导出私有业务数据、不调用模型；实现后的证据见 [验证记录](validation.md)。

## 决策及理由

### 1. 全部本地组件使用宿主机进程（2026-09-22 修正）

用户已明确取消本地 Docker 依赖。复用 `dev.sh` 的服务编排，Neo4j 从官方发行包安装，
GROBID 从官方 0.8.1 源码构建，使用源码默认 Wapiti 模型、pdfalto 和原生库；不改变解析 API。
官方 `Dockerfile.crf` 同样采用 Java 17 构建及运行这些资源，因此项目私有 Java 17 用于
GROBID，Java 21 继续供 Neo4j 使用；Gradle 固定为支持 Java 17 的 7.6.4。
不替换系统 Java、不修改用户 shell、不引入 Docker 命令回退。
Node 22、Python 3.12、MySQL 8.4 及其余存储版本保持原契约。
同包 Docker 目标仍属于独立待实现范围，不能与本地部署前置条件混合。

采用 Go 1.25.14、Neo4j 5.26.29、Qdrant 1.19.0、GROBID 0.8.1。实现已固定官方来源与
SHA-256/镜像摘要，Miniforge、Go 和 Qdrant 的实际下载校验通过；完整安装入口和平台
矩阵仍单独验收。不使用浮动 `latest` 或未经校验的代理下载。

新安装来源的证据为官方 `neo4j/docker-neo4j-publish` 的 5.26.29 Dockerfile 中的发行包
URL/SHA-256、GROBID 0.8.1 标签源码与 Gradle 官方摘要。官方地址不可达仍须明确失败，
不跳过校验、不假装部署完成；旧容器不会被本地启停命令删除或接管。

当前机器 Neo4j CDN 返回 403，但 `s3.eu-west-1.amazonaws.com/dist.neo4j.org/` 的同名
发行对象可下载，已验证其 SHA-256 与官方 Dockerfile 声明完全相同。将此地址列为该
制品的备用来源，只有传输失败才回退，摘要错误直接失败；不修改 DNS、系统代理或 TLS 校验。

### 2. 一份部署配置事实来源

Conda 由用户预先安装，不再自动下载 Miniforge 或升级 Conda。查找顺序为显式 `CONDA_EXE`、`PATH`、已有 `CONDA_ROOT`、常见安装位置、已有项目私有安装；最后一项仅复用历史安装。通过 `conda env list --json` 获取环境前缀；多处同名且无法唯一定位时要求显式选择，不猜路径。用 `conda info --base` 校验选中环境不指向 base；不兼容或查询失败在写入前阻断。所有 Python/pip 调用使用选定环境的绝对路径；Node、Java、MySQL、Redis 优先在同一环境提供，Go/Neo4j/Qdrant 放项目运行目录。

用户于 2026-09-22 确认这一分工。保留版本清单中历史 Miniforge 元数据只为避免改变
既有包/实例的版本摘要，不代表继续支持自动安装；没有 Conda 时由预检和安装器分别拒绝。
不采用自动 `conda init`、修改 base Python 或自动安装系统软件的方案。

Go 安装检查与 `goserver-run.sh` 共用 `environment.go_environment()`，避免两阶段使用
不同 GOPATH/GOPROXY 重复下载。默认保留原运行脚本的 `~/.local/gopath`、
`~/.cache/go-build` 和 `https://goproxy.cn,direct`；显式进程配置优先，不写全局设置。

配置生成器使用标准库解析和渲染，拒绝命令替换、反引号和任意 shell 代码；保留普通 dotenv 的引号与 `${NAME}` 引用语义。服务启动同样改用解析后的环境，不继续 `source .env`。新 `.env` 权限为 0600，旧 `.env` 字节保持不变；不兼容配置需要用户修正后重试。

### 3. 可校验源码包加数据库原生导出

源码采用干净 `HEAD` 的归档，不携带 `.git`、依赖安装目录、日志、PID、旧配置或整个 `.local`。MySQL 使用逻辑导出，Neo4j 使用停库后的业务数据库 dump，Qdrant 使用各集合快照及别名清单；不要复制运行中的存储文件。

只导出业务 MySQL 库，不导出 `mysql` 等系统库和数据库登录账号。携带业务例程、触发器、事件的定义；恢复时将旧 DEFINER 映射为目标项目服务身份，不授予全局权限。无法处理的跨库引用仍预检拒绝。事件调度总开关开启不等于有事件可执行：停应用前检查全实例事件和执行线程，导出前后及发布前复核；无启用事件且无执行线程时允许打包。调度关闭时仍检查残留执行线程；开启且存在启用事件时拒绝，不依据未经证明的剩余时间放行。

2026-09-21 用户授权实际运行 `make frozen` 并彻底修复。当前业务账号不能完整查看事件，
使用当前项目 `.local/my.cnf` 的 root 连接只读检查，核对 `server_uuid` 与业务连接相同，
确认全局 EVENT 权限和可读取执行线程；缺权限或连到其他实例时失败。管理员连接仅用于
元数据检查，不用于导出业务数据，不改变权限、事件或调度开关。此修复不改变业务所有权。

当前实例存在 `backups`（历史迁移 SQL）和 `tmp`（pytest 临时目录），明确排除，不能把
旧凭据或测试副本装入当前业务快照；未知业务目录仍拒绝，目录检查提前到停应用前。
源服务恢复环境的 DEBUG 使用项目配置或应用默认 false，避免继承工具终端的 release 字符串
导致 Python 布尔设置校验失败；不修改用户 `.env` 或全局环境。

Neo4j 只迁移业务数据库，不搬运 `system` 库的认证状态；目标初始化新密码。向量库恢复集合配置、点和别名，不调用 Embedding 重建索引。

### 4. 停写窗口和草稿保留

打包记录原服务状态，暂停调度与应用入口，等待已有请求结束；检测 Redis/RQ 运行、排队、延迟重试作业和数据库侧未完成工作。存在作业则恢复原状态并失败，不强杀。完全停写后再导出各存储；等待宽限期为 300 秒，超时不发布包。仅停止/恢复确认属于本项目的进程。

Redis 采用上传业务键白名单导出结构化 JSON，只保留状态、草稿与有效索引；不导出 RQ 序列化任务、锁、会话、验证码或 `upload:llm:*`。保留绝对到期时刻和业务清理时刻，不因搬迁延长原有保存期限；恢复时跳过已经过期的记录并报告数量。草稿重新发起解析时需重新提供模型凭据。

### 5. 同版本恢复和明确迁移模式

迁移包要求源库版本集合与该源码受测头集合相等，且真实结构符合迁移/模型约束。不一致时停止打包，列出差异，让独立维护流程处理；打包本身不升级源库。

恢复 SQL 后只核验版本集合和真实结构，不执行历史升级，也不 stamp 伪造完成。新空库则按源码固定头集合执行从零迁移并填充基础数据。`dev.sh` 对已经验证的便携部署走同一版本检查入口，不能又调用旧迁移逻辑改变数据。旧部署未带便携记录时保留其原行为，本 Feature 不批量迁移旧实例。

明确不把 `create_all` 加 `stamp` 当作完整迁移，不自动合并 Alembic 分支，不把全部历史迁移的破坏性动作搬到普通启动步骤。

### 6. 空目标和有限恢复重试

先预检，后在项目所有的临时目录恢复；未验证成功前不开放应用或 Worker。完成后把数据目录提升为本次实例，记录包摘要。中断发生在提升前时，只重建本次带所有权标记的临时目录；提升后重试只检查和启动，不重新导入。

不实现合并或覆盖恢复。配置或已有数据无法判定归属时直接拒绝。此限制降低误覆盖成本，也使重复执行的行为可验证。

### 7. 敏感配置与能力报告

迁移业务账号及密码哈希，重新生成目标 JWT 和数据库凭据，旧登录令牌不继承。外部模型与 SMTP 凭据、运行时模型覆盖配置不装入普通包；目标可通过部署后的本地配置或已有管理界面补充。

报告分别列“基础服务”“AI/Embedding 配置”“SMTP 配置”“实际外部调用验收”；有配置不等于验收通过。普通健康检查不发模型请求、不发邮件。

## 用户纠正范围后的设计补充（2026-09-21）

**已确认需求**：本地实例打包，同一份包用于本地或 Docker 部署；不增加 Docker 源导出。

**现状证据**：`Runtime.check_ports()` 原来无条件查询带目录哈希的 GROBID 容器；
`lib-local.sh` 只有便携部署记录存在时才使用该名称，旧实例使用 `scwiki-grobid`。
本次只读查询确认旧容器存在、新名称不存在，查询退出 1，复现用户错误。
打包不启停也不导出 GROBID，因此只在打包时排除该项检查；不放宽数据库/应用归属检查，
不通过给旧容器伪造项目标签或重建运行容器解决。

**拟采用的 Docker 恢复设计**：通用包保留数据库原生导出，不为 Docker 再生成一份 SQL。
复用既有 Compose 服务与 Dockerfile，增加目标适配器和恢复覆盖配置；共享存储/校验模块
负责业务数据语义，适配器负责容器执行、路径映射和生命周期。源码、数据库版本必须匹配；
从包内源码构建应用，不能把当前浮动镜像标签视为同一提交。

**需要改变的既有假设**：恢复工具不再总在宿主 Conda 中运行，目标文件路径不再总是宿主
`.data`，Docker `migrate` 不得在导入后继续自动执行历史升级。宿主挂载路径与容器 `/data`
须分别建模。凭据、空目标、业务所有权、草稿有效性和不覆盖规则不变。

上述 Docker 方案与 Plan 配套，待设计确认后进入 T031–T033；仅写入方案不表示已实现。

不采用“把包解压到 Docker 目录再直接 compose up”：当前 Compose 不消费 `.deployment`，
且会执行迁移任务；这无法证明业务数据导入、路径重定位或历史数据保持正确。

## 被拒绝的方案

### 唯一 `dist` 压缩包的自动发现（2026-09-22）

用户已明确要求只有一个包时自动解压恢复，多包要求人工移走多余包，不按时间选择。
复用现有 tar 包及 `.deployment` 清单，不引入另一套 SQL 导入器。只扫描 `dist` 直接子项，
校验文件不计数，常见但不支持的压缩格式必须报错，不能误认为无包并新建空库。
`BUNDLE` 保留显式来源能力，但不绕过多包限制；多个来源必须核对完整清单，不仅比较包 ID。
普通入口和 `CHECK_ONLY` 的包错误均只报告，不修改原部署报告、配置或数据。

当前真实包来自旧提交，服务版本清单中的 Neo4j/GROBID 安装描述也不同；自动发现本身
不能证明数据兼容。源码严格一致性保护本阶段保持，保留当前源码的兼容数据恢复另行确认。

### 实现核验补充：返修草稿的路径与冲突基线

`backend/services/paper_revisions.py:fingerprint` 将 `PaperFile` 行纳入返修并发校验，
其中包含 `stored_path`。直接重定位绝对路径会让原本有效的返修草稿返回 409。
恢复仅在尚未开放应用的临时库中，先记录源端仍匹配当前论文、revision 和审核状态的草稿，
重定位后为这些草稿重算并发基线；原有冲突、草稿内容中的科学指纹、审批和历史记录保持原状。
这是一回恢复中的适配，不改变线上返修读取/提交规则，也不为陈旧草稿恢复有效性。
两种草稿表的实际主键分别为 `scientific_upload_drafts.task_id` 和
`paper_revision_drafts.paper_id`，路径迁移不得假定它们有 `id` 列。

- 压缩整个仓库目录：会混入旧路径、密钥、数据库裸目录及不可搬迁的运行环境。
- 只导出已有 JSON 导入器覆盖的用户/论文：遗漏动态表单、审核历史、社区、版本草稿等表。
- 把 Redis 直接当缓存丢弃：会丢失仍保存在 Redis 的草稿。
- 原样恢复 Redis RDB：可能携带用户 API Key，重放旧队列和锁。
- 所有失败都跳过：无法保证最终实例完整，也难以定位错误。

## 实施验收门槛

固定依赖下载/安装、MySQL 从零多头迁移、源数据库结构一致性、Neo4j/Qdrant 原生导出
恢复、持久路径字段登记、RQ 与新闻队列停写均需真实行为证据。已通过和待补齐范围分别
见 [验证记录](validation.md) 与 [Tasks](tasks.md)，不得以静态检查代替。
