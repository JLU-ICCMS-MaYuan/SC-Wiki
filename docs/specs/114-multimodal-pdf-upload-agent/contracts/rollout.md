# Rollout 契约

## 阶段

- `legacy`：仅旧链路。
- `shadow`：旧链路结果服务用户，新链路只保存对照结果。
- `gray`：按允许名单/比例启用新链路，失败任务回退旧链路。
- `default`：新链路默认，旧链路仍可按任务回退。

## 门禁

默认切换必须有 50 篇评测报告，满足 SC-001～SC-003，且失败收敛和旧缓存回归通过。配置不满足门禁时拒绝进入 `default`。用户不选择解析器。

## 观测

每次运行记录阶段、解析器、版本、fallback、错误码、耗时、token/资源统计和最终路径；不记录密钥或思维链。
