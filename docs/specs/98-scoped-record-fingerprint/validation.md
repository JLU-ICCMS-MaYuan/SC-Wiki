# 验证记录

2026-09-09，使用当前 sc-wiki Conda 环境和 `.env` 指向的 scwiki MySQL；未建立临时数据库，未执行结构迁移。

## 回归

修复前 `test_issue98_submit.py` 三项均失败：真实草稿提交重现 `uq_property_records_source`，来源冲突误报记录键，以及跨状态同名键导致错误索引。

修复后该文件与 `test_issue90_properties.py` 共 7 项通过。MySQL 集成检查覆盖：

- 真实草稿 `_create_pending_paper` 完整事务，三条 Tc 的值和数量一致。
- 跨状态及跨模块同名局部键的缺省指纹互异。
- 重复保存不增加记录；编辑科学值及省略指纹的旧载荷保留已有数据库指纹。
- 同一真实 Evidence 可以绑定两条不同状态的记录。
- 新建记录的显式指纹保留。
- 测试外层事务最终回滚，MySQL 无任务对应论文，Redis 草稿深比较未变。

跨模块探针使用事务内创建的已校验定义；当前库没有已发布的电子模块记录定义，探针与定义均回滚。

正式提交后，科学草稿、#90 与 #98 非数据库回归共 16 项通过；需要未提交任务的 MySQL 集成项默认跳过（此前已用同一真实任务验证通过）。`git diff --check` 通过，本地 Python 服务热重载后正常启动。

## 正式提交

通过 `submit_upload_draft` 真实入口提交任务 `92127c24cf034ac79845a8055ccc90b8`：返回 `paper_id=29`、`review_status=pending`。
Sn 3.78 K、Pb 7.19 K、SnHg 4.29 K 三条记录分别保留，三个来源指纹不同。
再次调用同一入口返回相同论文 ID，数据库仅一篇论文对应该任务。审核快照的用户数据与提交前草稿一致，Redis 临时任务按成功流程清理。

运行中仍有既有 `volume_angstrom3` 小数精度截断警告和晶体库弃用警告；它们没有阻断本次提交，来源身份修复未修改结构计算或列精度。
