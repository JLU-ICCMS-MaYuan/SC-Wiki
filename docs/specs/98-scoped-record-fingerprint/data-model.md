# 数据与生命周期

表结构不变。`record_key` 在所属模块内定位记录；模块属于材料状态；状态属于论文 revision。

| 写入情形 | 来源指纹 |
| --- | --- |
| 已存在记录 | 保留数据库中的值 |
| 新记录显式带指纹 | 沿用既有显式值与唯一约束 |
| 新记录未带指纹 | 状态主键、模块键、记录键组成的规范 JSON 数组哈希 |

相同论文或相同证据可以被多条记录共享，不通过指纹是否相同判断科学结果等价。修改科学值影响 `record_checksum`，不影响已存在的来源指纹。
