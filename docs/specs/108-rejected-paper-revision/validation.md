# 验证记录

日期：2026-09-18。验证对象是 #108 实现及从当前 HEAD 单独提取的 #108 改动；共享文件中的 #100/#109 等并行改动没有纳入该验证副本或提交。

## 环境与结果

- 在 `/tmp/scwiki-rejected-revision/` 启动独立 MySQL 8，绑定 `127.0.0.1:13318`，库名 `scwiki_revision_full_test`。未连接业务库，没有原上传 Redis 任务或文件审核快照。表由真实 Alembic 迁移创建，#90 按实际迁移脚本完成切换，再升级到 `20260918_0108`。
- Python：`backend/tests/test_paper_revisions.py`、`backend/tests/test_scientific_drafts.py`、`tests/01_decentralized_uploading/test_scientific_evidence.py`，合计 **41 项通过**。返修用例使用真实 HTTP、JWT、asyncmy/MySQL 事务、来源定位和版本级联。模型返回用固定引句替代，不调用外部模型。
- 补充旧管理员接口权限断言及历史字段提示后，最终返修专项 **21 项通过**；没有跳过跨 Go/MySQL 的审核用例。
- 前端：返修表单、上传分类、管理员科学数据展示和共享核对流程，合计 **32 项通过**。确认暂存只保存草稿、提交携带最新版本、冲突保留输入、入口服从服务端能力。分离共享改动后又运行返修表单 **4 项通过**。
- Go：权限、审核信息过滤、可见性和升版公开边界 **4 组通过**。Python 集成用例另外调用真实 Go `ReviewPaper`，验证重新送审后管理员再次拒绝、普通上传者不能审核或绕过草稿，随后用 Python 开启第二轮返修。
- 独立提交副本的 `npm run build` 通过。已有 3Dmol `eval`、包体积及依赖弃用提示；没有新增构建错误。

## 验收映射

| 要求 | 验证证据 |
| --- | --- |
| FR-001/010、SC-001 | 上传者、其他用户和未登录请求；三状态权限；旧管理员接口仍拒绝 rejected；Go 详情能力及旧 PATCH 拒绝绕过 |
| FR-002/003/009、SC-002 | 正式数据恢复、缺失分类提示、草稿更新时间改为三日前仍可恢复；保存不改正式数据；CIF/POSCAR 解析、上传归属及服务端来源登记 |
| FR-004/005/008、SC-003 | 变化摘要使核对失效；同一 ID 升到第二版；两条物性、多结构、原格式、元数据、父子关系和结构引用保留；来源、审核与定义事件历史保留 |
| FR-006/007、SC-004 | 真实重建失败回滚原科学实体；顺序和并发重试均只升版一次；同版本管理员书目/结构编辑、状态变化、旧窗口保存、迟到核对均检测冲突 |
| US1/US2 | 暂存 → 来源核对 → 重新送审 → Go 管理员再次拒绝 → 下一轮返修；前端单独验证保存与送审操作分离 |

## 验证边界与旧用例

没有部署、推送或修改生产数据库，也没有声称外部模型或生产索引已经验收。跨服务用例的第二次审核选择“拒绝”，使用真实审核事务；事务外索引发布及上传文件清理由本地接收器隔离。

曾尝试运行旧 `backend/tests/test_scientific_draft_rewrite.py`，其初始化会清空指定测试库并执行 `upgrade head`，被工作区另一任务的未提交迁移分支阻断，且样本仍使用 #90 之前的全局材料字段。因此这些旧用例没有计入通过数。本次重新建立隔离库，在当前表结构上直接验证所需重写、回滚、旧管理员状态限制和跨服务路径。

文档相对链接与本次提交差异检查通过；README、上传 Overview、论文审核 Overview 已同步，旧 #76 的历史决策原文保留，由本 Spec 明确补充返修用例。
